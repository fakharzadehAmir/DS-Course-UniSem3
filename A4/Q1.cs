using System;

namespace A4
{
    class Q1
    {
        static void Main(string[] args)
        {
            var numberSize = Array.ConvertAll(Console.ReadLine().Split(), int.Parse);
            var numbers = new int[numberSize[0]][];
            for (var i = 0; i < numberSize[0]; i++)
            {
                
            }
    }
    }
}
